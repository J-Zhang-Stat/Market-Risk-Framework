# Notebook Notes

The original exploratory notebook is kept locally as `Market_risk (1).ipynb` for provenance, but it is ignored by Git because it contains exploratory outputs and local notebook state. The GitHub-facing implementation lives in `src/market_risk` because it is easier to review, test, and run from a clean environment.

Recommended reviewer path:

1. Use the notebook only as background research context.
2. Use `scripts/run_analysis.py` as the reproducible workflow.
3. Use `reports/summary.md` and `reports/figures/` for generated outputs.
